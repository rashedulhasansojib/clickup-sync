# Keep the build context small and deterministic.
# node_modules are reinstalled inside the image; dist is rebuilt.
node_modules
**/node_modules
dist
**/dist
*.tsbuildinfo
.git
.gitignore
coverage
*.log
/logs
.DS_Store
# Never ship real secrets into an image layer
.env
.env.local
.env.*
!.env.example
